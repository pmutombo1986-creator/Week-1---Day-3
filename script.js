const colors = ['red', 'orange', 'yellow', 'blue', 'green']; /* declare an array of five strings */

console.log(colors[1]); /* display "orange" without a loop */

const userColor = colors[1]; /* store "orange" into a variable */

console.log(userColor); /* display the value stored in the item variable */